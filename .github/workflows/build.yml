name: CI

on:
  push: 
    branches: main
  workflow_dispatch:
  pull_request:
    branches: master

jobs:
  build:
    name: Build & Tests
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v2
      - name: Run build
        run: echo "Building..."
      - name: Run tests
        run: echo "Tests..."
